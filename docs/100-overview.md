---
title: Overview
vpath: /
vslug: overview
tags:
  - docs
  - read this first
draft: false
chapter: 100
---

`mdf` is a lightweight CLI for developers who keep knowledge in Markdown tracked
by git.

Note: mdf 0.x is experimental and may change over time.

## What mdf does

`mdf` uses Markdown plus a small front‑matter block to organize knowledge in a
way that’s friendly to both humans and AI agents.

### Front matter

Front matter is a YAML block at the top of a file, separated from the body by a
blank line. It stores document metadata.

```mdx
---
title: Let's build a CLI to manage knowledge in Markdown files stored in git
status: todo
author: "@stakme"
updated_at: 2025-10-12T00:09:21.406Z
---

# Introduce code highlighting like GitHub
```

### Schema and validation

Define a schema per directory in the config file. Schemas make it easy to
organize, filter, validate, and publish notes—without standing up a server.

```ts
docs: defineSchema({
  glob: "docs/**",
  schema: z.object({
    title: z.string().min(1).default("title"),
    description: z.string().optional(),
    tags: z.array(z.string()).default(() => []),
    draft: z.boolean().default(true),
    chapter: z.number().default(0),
    updated_at: z.date().default(() => new Date()),
  }),
  sort: (a, b) => a.chapter - b.chapter,
  visibleFields: ["tags"],
}),
```

### CLI workflows

Create, list, validate, update, and export notes from the CLI. Coding agents can
read the same workspace directly from disk—no MCP bridge or HTTP API required.

```sh
# Create a new file with front matter. You can also use a template to seed the body.
$ npx @stakme/mdf new --fm 'title=something new' ./docs
docs/01K7AW2SKXMNENW0CQ6BR4XG5B.md

# List files by front‑matter filters.
$ npx @stakme/mdf list --filter "draft=true" ./docs
./docs
└── something new (./docs/01K7AW2SKXMNENW0CQ6BR4XG5B.md)

# Update an existing file’s front matter.
$ npx @stakme/mdf update --fm draft=false --fm updated_at docs/01K7AW2SKXMNENW0CQ6BR4XG5B.md
Updated ./docs/01K7AW2SKXMNENW0CQ6BR4XG5B.md
```

### Viewer and export

Preview notes in a local viewer, then export a static site. The page you’re
reading can be generated with the export command.

```sh
$ npx @stakme/mdf viewer ./docs
```

## Why Markdown + front matter

In a fast‑moving AI era, tools come and go; plain text endures. Markdown with
simple front matter stays readable, portable, and diff‑friendly. Instead of
building new silos for each agent, `mdf` offers a durable language both agents
and humans can work with.

## Core capabilities

- Front‑matter filtering: target documents by fields, not just text (for
  example, `status=todo and owner=me`).
- CLI‑first workflows: create, list, validate, update, and export against
  predictable schemas.
- Agent‑friendly access: everything lives on disk, so automation can read it
  without services.
- Lightweight viewer: `mdf viewer` launches a simple web UI for review and
  prioritization.

## Everyday examples

- Backlog triage with an alias:
  - In `.config/mdf.mts`, add `todo: list --filter "status=todo" ./TODO`.
  - Run `mdf run todo` to list unfinished tickets by front matter.
- Docs export: run `mdf export ./docs` to generate a static site, filtered your
  way.

## Documentation map

- Getting Started: installation, configuration, and the core authoring workflow.
- Commands: focused pages under `commands/` with examples and recommended flags.
- Automation Guides: exporting a docs site and wiring it into CI (including
  GitHub Pages).
- Recipes: common daily tasks like grooming TODO queues or preparing release
  notes.

This page is generated by `mdf export`. Run `mdf export ./docs` to (re)build the
static output in `./docs` for GitHub Pages. Edit the source notes under `/TODO`,
not the generated files.
